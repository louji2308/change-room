{
  "name": "@change-room/state",
  "version": "0.1.0",
  "description": "Change Room operational state model — versioned, transition-safe system state.",
  "type": "module",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "typescript": "^5.1.0"
  }
}
