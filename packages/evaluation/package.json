{
  "name": "@change-room/evaluation",
  "version": "0.1.0",
  "description": "Change Room Phase 16 — blind evaluation driver: a real agent-driven operator and a control-layer policy surface over the scenario suite with the full Phase-16 metric set.",
  "type": "module",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "@change-room/agent": "workspace:*",
    "@change-room/control": "workspace:*",
    "@change-room/scenarios": "workspace:*",
    "@change-room/simulator": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "typescript": "^5.1.0"
  }
}